# Full Admin Functionality and Mobile Layout Audit

วันที่เริ่มตรวจสอบ: 17 สิงหาคม 2026

## รอบที่ 1: การเปิดหน้าเมนู

ระบบทดสอบการเปิดเมนู Admin ทุกเมนูที่แสดงในแถบนำทางจำนวน **23 เมนู** โดยใช้บัญชีผู้ดูแลจำลองและไม่มีการส่งคำสั่งแก้ข้อมูลจริง ผลเบื้องต้นพบว่าทุกเมนูเปิด section ปลายทางและมีปุ่มกลับเมนู Admin ได้ โดยไม่พบ JavaScript error ระหว่างการเปิดหน้า

เมนู **AI Workspace** ตรวจซ้ำแล้วพบว่า section มีเนื้อหาครบตามปกติ โดยผลอ่านเป็นศูนย์ในรอบแรกเกิดจากตัวตรวจรอบแรกมองเฉพาะ container แบบ `.panel` ขณะที่ AI Workspace ใช้ `.aiw-shell` จึงไม่ใช่หน้าว่างจริง

รอบคลิกจริงได้เปิดและกดปุ่ม **กลับเมนู Admin** ทีละรายการครบทั้ง 23 เมนู ผลคือทุกเมนูมี section ปลายทาง มีเนื้อหาขั้นต่ำ มีปุ่มกลับ และกลับสู่เมนูหลักโดยไม่มี class หน้าเมนูย่อยค้างหรือ JavaScript error

ประเด็นที่ต้องแก้ต่อจึงอยู่ที่ **การแสดงผลมือถือและความชัดเจนเมื่อไม่มีข้อมูล** โดยเฉพาะหน้าตารางออร์เดอร์ การเงิน และ error center ที่ยังใช้ตารางกว้างและทำให้ผู้ใช้เห็นหัวข้อบางคอลัมน์ไม่ครบในจอแคบ

## รอบที่ 2: ปุ่มและตัวกรอง

การตรวจ handler ของ controls ในทั้ง 23 เมนูไม่พบ inline action ที่อ้างถึงฟังก์ชันไม่อยู่จริง ปุ่มที่ดูเหมือนไม่มี `onclick` ส่วนใหญ่เป็นปุ่ม submit ของฟอร์ม หรือถูกผูกด้วย event listener โดยตรง

ได้คลิกทดสอบตัวกรองออร์เดอร์และแท็บประวัติการตรวจสลิปแล้ว ทั้งสองเปลี่ยนสถานะ active และแสดงมุมมองเป้าหมายได้จริง ส่วน Finance และ Creator ใช้โครงสร้างแท็บคนละรูปแบบจาก selector ตรวจเบื้องต้น จึงต้องตรวจ UI จริงของแต่ละหน้าและเพิ่มตัวตรวจให้ตรงโครงสร้างในรอบต่อไป

## รอบที่ 3: โครงสร้างตารางบนมือถือ

เพิ่มตัวจัดรูปแบบตารางกลางให้ทุก section ของ Admin เตรียม label ของแต่ละคอลัมน์ไว้กับข้อมูลแถว และตรวจในหน้าออร์เดอร์แล้วว่าแพตช์ถูกโหลด ตารางได้รับ class สำหรับการ์ดมือถือ และไม่มี JavaScript error ระหว่างเปิดเมนู

การ sweep หลังเพิ่มแพตช์ได้เปิดและกลับครบ 23 เมนูอีกครั้ง โดยทุกหน้าที่มีตารางได้รับ class การ์ดมือถือครบ ไม่มี error และไม่พบลูปจาก MutationObserver

หน้ารายการร้านค้าไม่ได้แสดงช่องอัปโหลดหรือปุ่มตำแหน่งโดยตรง เนื่องจาก controls เหล่านี้อยู่ใน **หน้ารายละเอียดร้าน** ซึ่งเป็น modal แยก จึงต้องทดสอบต่อในระดับ detail modal พร้อมข้อมูลร้านจริง

เปิด detail modal ของร้านจริงสำเร็จและยืนยันว่ามีแท็บข้อมูลร้าน รูปและสื่อ และเวลา/โลเคชัน แต่เมื่อเปิดแท็บ **รูปและสื่อ** ยังไม่พบช่องเลือกไฟล์หรือปุ่มกล้อง/คลังใน DOM แม้ตัวบีบอัดรูปของระบบพร้อมใช้งาน จึงระบุเป็นประเด็นต้องตรวจ renderer และคืน controls ให้ใช้งานได้ในรอบแก้ไข

การตรวจ renderer พบสาเหตุจริง: regex ตรวจรูปแบบ `data:image` มี backslash ซ้ำ ทำให้ JavaScript อ่าน `i` เป็นตัวแปรและเกิด `ReferenceError` ทุกครั้งที่เปิดแท็บรูปและสื่อ นี่คือสาเหตุที่ทำให้ modal แสดงเหมือนหน้าว่างหรือค้าง

แก้ regex และทดสอบในเบราว์เซอร์ใหม่แล้ว: detail modal เปิดได้, แท็บรูปและสื่อแสดงช่องเลือกไฟล์รูปหลักและภาพพื้นหลัง, มีปุ่มเลือกจากคลังและถ่ายรูปด้วยกล้อง, แท็บเวลา/โลเคชันแสดงปุ่มใช้ตำแหน่งปัจจุบันและเลือกพิกัดบนแผนที่ครบ โดยไม่มี JavaScript error

การค้นหารูปแบบเดียวกันพบ regex รูปแบบ `data:image` ที่ escape ซ้ำอีกหนึ่งจุดในหน้าหลัก จึงแก้พร้อมกันและเพิ่ม regression test ครอบคลุมทั้งสองตำแหน่งแล้ว

การทดสอบแท็บแบบวนครบทุกหน้าในเบราว์เซอร์ทดสอบใช้เวลาเกินกำหนด เพราะแท็บประวัติของบางคิวเริ่มคำขออ่านข้อมูลจริงที่ไม่มี session สำหรับ audit จึงไม่ใช้ผล timeout ตีความว่า UI ค้าง การเปิดเมนูและการกลับหน้า 23 เมนูยังผ่านในการ sweep ก่อนหน้า และจะทดสอบเฉพาะเส้นทางสำคัญบน session จริงหรือจำลองที่ตอบกลับได้ในรอบ regression

หลังแก้ไขได้เปิดและกดกลับเมนู Admin ครบทั้ง 23 เมนูอีกครั้ง ผลคือทุกเมนูมี section ปลายทาง เนื้อหา และปุ่มกลับครบ โดยไม่พบ JavaScript error หรือหน้าค้างในรอบตรวจ

## การเผยแพร่

GitHub Pages build ของ commit `778a3a5` เสร็จสมบูรณ์แล้ว และหน้าสาธารณะยืนยันการโหลด `admin_contact_ui_patch.js` เวอร์ชันแก้ไข รวมถึง `admin_mobile_layout_patch.js` โดยไม่มี runtime error ที่ตรวจพบ
