# Admin Today and History Workspace — Validation Notes

วันที่ตรวจสอบ: 17 สิงหาคม 2026

การตรวจด้วยหน้าเว็บในเครื่องยืนยันว่า หน้าแรกของลูกค้ายังโหลดรายการบริการ ร้านค้า และปุ่มตะกร้าได้ตามปกติหลังโหลดแพตช์ใหม่ โดยไม่พบข้อผิดพลาด JavaScript ที่แพตช์บันทึกไว้

| จุดที่ตรวจ | ผลลัพธ์ |
|---|---|
| หน้า Admin > ออร์เดอร์ | เปิดเป็นหน้าถัดไปตามรูปแบบมือถือเดิมได้ |
| มุมมองเริ่มต้น | แท็บ **งานวันนี้** ถูกเลือกอัตโนมัติ |
| ตัวกรองออร์เดอร์ | มีตัวเลือกงานวันนี้ งานค้าง ประวัติ และทั้งหมด |
| จำนวนงาน | แสดงจำนวนรายการของมุมมองปัจจุบันแยกจากข้อมูลเก่า |
| หน้าแรกลูกค้า | ยังแสดงปุ่มบริการ โปรโมชั่น ร้านค้า และตะกร้าได้ |

หลังเปลี่ยนเวอร์ชันสคริปต์เพื่อให้โหลดแพตช์ล่าสุดแล้ว ได้ตรวจซ้ำว่าแท็บ **งานวันนี้** ยังถูกเลือกเป็นค่าเริ่มต้น และตารางที่ไม่มีข้อมูลมีเพียงข้อความต้นทางหนึ่งรายการ โดยไม่ซ้อนข้อความ “ไม่มีรายการในมุมมองนี้” เพิ่มอีกแถว

เกณฑ์ออร์เดอร์ถูกยืนยันเพิ่มเติมว่า งานที่ส่งสำเร็จหรือปิดแล้วจะอยู่ใน **ประวัติ** เสมอ แม้ถูกปิดภายในวันเดียวกัน เพื่อไม่ให้ปนกับรายการที่ Admin ยังต้องติดตามในวันนี้

การทดสอบด้วยรายการจำลอง 4 รายการให้ผลตรงตามเกณฑ์: งานวันนี้ 1 รายการ, งานค้างจากวันก่อน 1 รายการ และประวัติรายการที่ปิดแล้ว 2 รายการ ซึ่งรวมงานที่ปิดภายในวันนี้ด้วย

การเปิด GitHub Pages ก่อน build เสร็จยังไม่พบตัวแปรแพตช์ในหน้าเว็บสาธารณะ จึงตรวจสถานะการเผยแพร่ต่อจน build ของ commit `437f23f` สำเร็จ หลังจากรีโหลดหน้าเว็บสาธารณะยืนยันได้ว่าสคริปต์ `admin_today_history_patch.js?v=admin-today-history-v3` ถูกโหลดและตัวแปรแพตช์พร้อมใช้งานแล้ว

การตรวจนี้ใช้สิทธิ์ Admin จำลองเฉพาะหน้าเว็บทดสอบในเครื่อง และไม่ได้เขียนหรือแก้ไขข้อมูล Supabase จริง
